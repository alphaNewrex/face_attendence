class Student {
  String name, id, image, attend, parentId, pId,cName;

  Student({
    required this.name,
    required this.id,
    required this.image,
    required this.attend,
    required this.parentId,
    required this.pId,
    required this.cName,
  });
}
